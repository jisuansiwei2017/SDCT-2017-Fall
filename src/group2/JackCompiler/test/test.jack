class Test {


	method void test() {
		return;
	}
	

    function void main() {
	
	var Array a;
	var int length;
	var int i, sum;

	
	let length = Keyboard.readInt("H");
	
	
	let a = Array.new(length);
	let i = 0;

	
	
	while (i < length) {

	    let a[i] = Keyboard.readInt("E");
		
	    let i = i + 1;
	}
	
	let i = 0;

	
	let sum = i + (Keyboard.readInt("R"));
	
	
	while (i < length) {
	    let sum = sum + a[i];
	    let i = i + 1;
	}
	
	do Output.printString("T");
	do Output.printInt(sum / length);
	do Output.println();
	
	
	return;
    }
}